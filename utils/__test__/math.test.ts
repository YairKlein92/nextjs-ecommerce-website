import { add } from '../math';

// test('add two nums together', () => {
//   expect(add(1, 1)).toBe(2);
// });

// test('throw an error if arguments are not numbers', () => {
//   expect(() => add(1, '1')).toThrow('pass only numbers');
//   expect(() => add(1, 'asdasd')).toThrow('pass only numbers');
//   expect(() => add(1, true)).toThrow('pass only numbers');
// });
